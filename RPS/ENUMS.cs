public enum signs
{
    EMPTY = 0,
    ROCK = 1,
    SCISSORS = 2,
    PAPER = 3
}
public enum hands
{
    LEFT = 1,
    RIGHT = 2
}
public enum shots
{
    NO = 0,
    EMPTY = 1,
    FULL = 2
}
public enum dirs
{
    BACK,
    FORWARD
}